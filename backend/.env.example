# 模块后端环境变量（部署时由主站 Deploy Worker 注入，本地开发可手动设置）

# 与主站共享的签名密钥，用于校验 X-PT-User-Signature（务必与主站 TOYBOX_MODULE_SIGN_KEY 一致）
MODULE_SIGN_KEY=dev-insecure-key

# 模块 id
MODULE_ID=welcome

# 若模块需要数据库（本模板未用），主站会注入独立库连接：
# DATABASE_URL=postgresql://module_welcome_user:password@postgres:5432/module_welcome_db
